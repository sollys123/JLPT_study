# 复习调度说明

紫藤的长期记忆状态使用 **FSRS-6** 的公开公式和 21 个默认参数。学习阶段的短间隔按钮行为则遵循 Anki 手册：默认学习步骤为 `1,10` 分钟，第一次学习步骤按“困难”时取前两个步骤的中点，即 6 分钟；处在后续学习步骤时按“困难”会重复当前步骤。忘记内容必须按“重来”，不能用“困难”代替。

默认目标记忆率为 90%。这通常只需要和每日新卡上限一起调整。底层 21 个参数不应凭感觉手改。

## 本项目没有自创间隔阶梯

- 新卡、学习中、复习、重学中分别记录。
- 长期复习间隔由稳定性、难度、可回忆率和目标记忆率计算。
- 每次评分都保存前后记忆状态、耗时和算法版本。
- 同源卡可延迟到次日；遗忘卡可自动暂停。
- 默认不启用随机模糊间隔，便于审计和测试。

## 参数优化

网页长期使用官方默认参数即可。积累较多真实复习记录后，项目附带了一个调用 Open Spaced Repetition 官方绑定的可选工具，而不是在网页里用简化算法假装优化：

```bash
npm install
npm run optimize-fsrs -- 你的紫藤完整备份.json
```

工具会生成一份含 21 个参数的 JSON。复习量很少时，它会给出警告，此时继续使用默认参数通常更稳妥。
